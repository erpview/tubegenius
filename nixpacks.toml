[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs_20", "python311", "ffmpeg", "yt-dlp"]

[phases.install]
cmds = ["cd server && npm install"]

[phases.build]
# Skip build phase - backend only, no frontend build needed

[start]
cmd = "cd server && node server.js"
