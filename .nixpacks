{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_20", "python311", "ffmpeg"]
    },
    "install": {
      "cmds": ["cd server && npm install"]
    }
  },
  "start": {
    "cmd": "cd server && node server.js"
  }
}
